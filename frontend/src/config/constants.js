/**
 * Constantes del sistema (Frontend)
 * Valores importantes que deben mantenerse consistentes
 */

// Costo de inscripción (IMPORTANTE: Este valor debe ser consistente con el backend)
export const COSTO_INSCRIPCION = 120;

// Capacidad máxima de clases (IMPORTANTE: Este valor debe ser consistente con el backend)
export const CAPACIDAD_MAXIMA_CLASE = 11;

